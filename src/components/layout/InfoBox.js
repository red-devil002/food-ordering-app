export default function InfoBox({childern}) {
    return(
        <div className="bg-blue-400 border border-blue-100 rounded-lg text-center mb-8 font-semibold w-32 ">
            {childern}
        </div>
    )
}